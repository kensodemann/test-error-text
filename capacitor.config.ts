import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "io.ionic.starter",
  appName: "test-error-text",
  webDir: "dist",
  bundledWebRuntime: false,
};

export default config;
